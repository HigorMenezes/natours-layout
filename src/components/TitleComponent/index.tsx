import React from 'react';

import { Container } from './styles';

import TitleInterface from './interface';

const MainTitleComponent = (props: TitleInterface) => <Container {...props} />;

export default MainTitleComponent;
