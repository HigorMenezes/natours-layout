import styled from 'styled-components';

import { colorWhite } from '../../theme/theme';

export const Container = styled.h1`
  color: ${colorWhite};
  text-transform: uppercase;
  margin-bottom: 6rem;
`;
